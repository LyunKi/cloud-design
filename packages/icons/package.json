{
  "name": "@cloud-design/icons",
  "description": "React Native UI Icons",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "version": "0.2.0",
  "scripts": {
    "generate": "pnpm exec ts-node scripts/start.ts",
    "test": "jest --config ../../jest.config.js",
    "test:watch": "jest --watch --config ../../jest.config.js",
    "test:updateSnap": "jest --config ../../jest.config.js --updateSnapshot",
    "build": "tsc -p ./tsconfig.build.json",
    "prepublish": "pnpm build"
  },
  "peerDependencies": {
    "react": "^18.2.0",
    "react-native": "^0.70.1"
  },
  "dependencies": {
    "react-native-svg": "^12.4.3"
  },
  "devDependencies": {
    "@types/react": "^18.0.15",
    "@types/react-native": "^0.70.1",
    "change-case": "^4.1.2",
    "react": "^18.2.0"
  }
}
