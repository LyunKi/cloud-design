{
  "name": "@ui-creator/common",
  "description": "UI Creator common package",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "version": "0.0.0",
  "scripts": {
    "build": "tsc -p ./tsconfig.build.json",
    "prepublish": "pnpm build"
  },
  "dependencies": {
    "@cloud-dragon/common-utils": "^0.0.2"
  },
  "devDependencies": {
    "@cloud-dragon/common-types": "^0.0.2",
    "@cloud-dragon/tsconfig-react": "^0.0.1"
  }
}
